# 🛒 Universal E-commerce Platform

Универсальный, масштабируемый движок интернет-магазина, построенный на связке **Django (Backend)** и **Next.js (Frontend)** с использованием Docker.


## 🚀 Технологический стек

### Backend (API)
*   **Python 3.11** + **Django 4.2**
*   **Django REST Framework** (API)
*   **PostgreSQL** (База данных)
*   **Gunicorn** (WSGI сервер)
*   **Swagger/OpenAPI** (Документация API)
*   Integrations: **Telegram Bot API** (Уведомления о заказах)

### Frontend (Client)
*   **Next.js 15** (App Router)
*   **React 19**
*   **Tailwind CSS** (Стилизация)
*   Optimizations: **SSR** (Server-Side Rendering) + **ISR** (Incremental Static Regeneration)

### Infrastructure
*   **Docker** & **Docker Compose**
*   **Nginx** (Reverse Proxy, SSL, Static files)

---

## 🛠️ Установка и Запуск (Локально)

### Предварительные требования
*   Установленный [Docker](https://www.docker.com/products/docker-desktop) и Docker Compose.
*   (Опционально) Make (для использования коротких команд).

### 1. Клонирование репозитория
```bash
git clone https://github.com/your-repo/bonafide55.git
cd bonafide55
```

### 2. Настройка окружения (.env)
Создайте файл `.env` в корне проекта. Вы можете скопировать пример:

```bash
# настройки Django
DJANGO_SECRET_KEY='ваш_секретный_ключ'
DJANGO_DEBUG=True
ALLOWED_HOSTS_STR=localhost,127.0.0.1,backend

# Настройки Базы Данных
POSTGRES_DB=bonafide_db
POSTGRES_USER=bonafide_user
POSTGRES_PASSWORD=password
POSTGRES_HOST=db
POSTGRES_PORT=5432
DATABASE_URL=postgres://bonafide_user:password@db:5432/bonafide_db

# Telegram (для уведомлений)
TELEGRAM_BOT_TOKEN=ваш_токен_бота

# Frontend
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME="My Super Store"
```

### 3. Запуск через Make (Рекомендуется)
Если у вас есть `make`, просто выполните:

```bash
make up         # Запустить проект в фоне
make logs       # Смотреть логи
```

### 3.1. Запуск через Docker Compose (Альтернатива)
```bash
docker-compose up -d --build
```

После запуска сайт будет доступен по адресу: **http://localhost:3000**
Админка: **http://localhost:8000/admin/** (или другой порт, если настроен Nginx).

---

## 📦 Деплой на Сервер (Production)

1.  Зайдите на сервер по SSH.
2.  Склонируйте репозиторий в папку `/app` (или другую).
3.  Создайте `.env` файл в корне проекта с боевыми настройками (`DJANGO_DEBUG=False`, реальные домены).
4.  Запустите проект:
    ```bash
    make deploy
    # или
    docker-compose up -d --build
    ```
5.  Создайте суперпользователя для входа в админку:
    ```bash
    make superuser
    ```

---

## 🔧 Полезные команды (Makefile)

Полный список команд можно увидеть, введя `make help`.

| Команда | Описание |
| :--- | :--- |
| `make build` | Собрать/Пересобрать контейнеры |
| `make up` | Запустить проект (в фоне) |
| `make down` | Остановить все контейнеры |
| `make restart` | Перезапустить проект (down + up) |
| `make logs` | Смотреть логи всех сервисов |
| `make logs-back` | Логи только бэкенда |
| `make migrate` | Применить миграции базы данных |
| `make superuser` | Создать администратора Django |

---

## 📂 Структура проекта

```
.
├── backend/                # Django проект
│   ├── shop/               # Основное приложение (товары, заказы)
│   ├── Dockerfile          # Инструкция сборки бэкенда
│   └── requirements.txt    # Python зависимости
├── frontend/               # Next.js проект
│   ├── src/app/            # Страницы (App Router)
│   ├── src/components/     # Компоненты React
│   └── Dockerfile.prod     # Инструкция сборки фронтенда
├── nginx/                  # Конфигурация Nginx
├── docker-compose.yml      # Описание сервисов
└── Makefile                # Команды автоматизации
```

## 📞 Контакты

Готовое решение для вашего бизнеса.
Масштабируемая архитектура, готовая к высоким нагрузкам.
