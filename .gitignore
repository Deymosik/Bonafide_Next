# --- Общее / Системные файлы ---
.DS_Store
Thumbs.db
*.log
*.pot
*.swp

# --- Переменные окружения и секреты (КРИТИЧНО) ---
.env
.env.*
.env.prod
.env.local
.env.development.local
.env.test.local
.env.production.local
# Исключаем локальные переопределения докера
docker-compose.override.yml

# --- Настройки IDE (по желанию, но рекомендуется) ---
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# ==========================
# BACKEND (Django / Python)
# ==========================
# Байт-код и кэши
__pycache__/
*.py[cod]
*$py.class

# Виртуальное окружение (если разворачивал локально без докера)
venv/
env/
.venv/

# Django
*.sqlite3
db.sqlite3
# Собранная статика (создается командой collectstatic)
backend/staticfiles/
# Логи ошибок
backend/django_errors.log

# Медиа файлы (загруженные пользователями)
# Игнорируем содержимое, но оставляем саму папку
backend/media/*
!backend/media/.gitkeep
# Если папка media создается в корне (зависит от настроек volume)
media/*
!media/.gitkeep

# Резервные копии (созданные через админку)
backend/media/backups/
media/backups/

# Celery (если добавишь в будущем)
celerybeat-schedule
celerybeat.pid

# ==========================
# FRONTEND (Next.js / Node)
# ==========================
# Зависимости
frontend/node_modules/
node_modules/

# Next.js билд
frontend/.next/
.next/

# Production билд (если используешь next export)
frontend/out/
out/

# Логи npm/yarn
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Файлы тестирования (покрытие)
coverage/

# Временные файлы Next.js
.vercel

# ==========================
# INFRASTRUCTURE / DOCKER
# ==========================
# SSL сертификаты (НИКОГДА не коммитить в git)
nginx/ssl/
*.pem
*.key
*.crt

# Локальные данные базы (если volume примонтирован в папку проекта)
postgres_data/
pgdata/